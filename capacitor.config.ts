import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lista_Tareas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
